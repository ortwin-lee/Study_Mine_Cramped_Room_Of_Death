export enum SHAKE_TYPE_ENUM {
    TOP = "TOP",
    BOTTOM = "BOTTOM",
    LEFT = "LEFT",
    RIGHT = "RIGHT",
}

export enum SCENE_ENUM {
    Loading = "Loading",
    Start = "Start",
    Battle = "Battle",
}
